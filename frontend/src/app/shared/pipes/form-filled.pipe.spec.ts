import { FormFilledPipe } from './form-filled.pipe';

describe('FormFilledPipe', () => {
  it('create an instance', () => {
    const pipe = new FormFilledPipe();
    expect(pipe).toBeTruthy();
  });
});
